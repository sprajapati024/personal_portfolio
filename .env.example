# OpenAI API Configuration
# This key is used server-side in Vercel serverless functions
# DO NOT use VITE_ prefix - that would expose it to the browser
# Add this to Vercel: Settings → Environment Variables → OPENAI_API_KEY
OPENAI_API_KEY=your_openai_api_key_here

# OpenWeatherMap API Configuration
# Free API key from https://openweathermap.org/api
# Used for weather widget data
# Add this to Vercel: Settings → Environment Variables → OPENWEATHER_API_KEY
OPENWEATHER_API_KEY=your_openweather_api_key_here
